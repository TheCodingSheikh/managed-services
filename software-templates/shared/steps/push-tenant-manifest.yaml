id: push-tenant-manifest
name: Push Tenant Manifest
action: publish:github:pull-request
input:
  repoUrl: github.com?owner=thecodingsheikh&repo=managed-services
  branchName: create-${{ parameters.name }}-tenant
  sourcePath: ./output
  targetPath: releases/tenant/${{ parameters.name }}
  title: "create ${{ parameters.name }} tenant"
  description: "create ${{ parameters.name }} tenant"
  gitAuthorName: ${{ user.entity.metadata.name }}
  gitAuthorEmail: backstage@thecodingsheikh.io
