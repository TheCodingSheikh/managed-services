id: fetch-skeleton
name: Scaffold Project Files
action: fetch:template
input:
  url: ./skeleton
  targetPath: ./output
  values:
    params: ${{ parameters }}
    owners: ${{ steps.fetch-tenant.output.entity.spec.owners }}
