apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: managed-services
  description: All managed service templates
spec:
  targets:
    - ./tenant/template.yaml
    - ./webapp/template.yaml
    - ./postgres/template.yaml
    - ./full-stack/template.yaml
